name: common
description: Common logic which can be used in all project packages.

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  fast_immutable_collections: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  glados: ^1.1.7