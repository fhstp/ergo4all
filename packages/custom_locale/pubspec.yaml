name: custom_locale
description: Storage logic for local custom locale.

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter